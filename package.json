{
  "name": "pasada-admin-application",
  "version": "0.1.0",
  "description": "Pasada Admin Application - Flutter Web App",
  "scripts": {
    "build": "flutter build web --release",
    "dev": "flutter run -d web-server --web-port 3000",
    "clean": "flutter clean",
    "get": "flutter pub get"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "devDependencies": {
    "@vercel/static": "^3.0.0"
  }
}
