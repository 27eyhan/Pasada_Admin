{
  "version": 2,
  "installCommand": "if [ -d flutter ]; then (cd flutter && git fetch origin stable && git checkout stable && git pull); else git clone -b stable https://github.com/flutter/flutter.git; fi && flutter/bin/flutter config --enable-web && flutter/bin/flutter precache --web && flutter/bin/flutter --version && flutter/bin/flutter pub get",
  "buildCommand": "flutter/bin/flutter build web --release",
  "outputDirectory": "build/web",
  "framework": null,
  "routes": [
    { "handle": "filesystem" },
    { "src": "/.*", "dest": "/index.html" }
  ]
}


